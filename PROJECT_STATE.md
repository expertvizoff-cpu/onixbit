# Onixbit Project State

Last updated: 2026-09-01

Shared hub: `/home/aleksander/projects/astra-command`

## Company Requisites Publication — 2026-09-01

- Replaced the old public company-card PDF with the approved new Onixbit
  requisites letterhead.
- Added direct PDF download actions to the About page, Contacts page and footer
  without changing routes, forms or the production information architecture.
- The briefly published editable DOCX and every public link to it were removed
  on Aleksander's explicit request. The public document is PDF-only.
- Pre-publication checks passed: `npm run lint`, `npm audit --omit=dev` and
  `npm run build` on Next.js 16.2.12. Browser automation was not run on the
  shared server under the project resource-hygiene rule; live static-file and
  page smoke checks are required after deployment.
- The first CI attempt was stopped by three newly published production audit
  findings. `npm audit fix` refreshed only compatible transitive versions in
  `package-lock.json`; the production audit now reports zero vulnerabilities.
  The full development audit still reports the known `extract-zip` chain under
  Lighthouse/Puppeteer and requires a separate breaking tooling change.
- Rollback target for the PDF-only follow-up is public `origin/main` commit
  `6e26aa1`.

## Purpose

This file is the durable handoff for Codex chats. If a chat is lost, compacted, or restarted, start the next chat by asking Codex to read this file first.

Shared rules for handoff and live-site safety live in Astra Command:

- `/home/aleksander/projects/astra-command/memory/recipes/project-state-handoff-lifecycle.md`
- `/home/aleksander/projects/astra-command/memory/recipes/live-site-change-safety.md`

## Current Git And Production

- The last externally verified production baseline before this workflow-only
  follow-up is `a5dc37662173d2edcf83704cc661f9e126d47dc6`; read the current exact
  `main` SHA from Git/GitHub instead of copying it from this handoff.
- CI run `30486065152` and deploy run `30486061795` completed successfully on
  2026-07-29. Homepage, `/api/health`, robots, sitemap, canonical `www`
  redirect and `https://media.onixbit.ru/healthz` passed external smoke.
- Rollback target for this maintenance release is
  `55d9328e5c89d52940551c643f67775e13dd2ade`.
- The backup branch `astra/git-rollout-2026-07-29` remains at
  `911b9781ad6ee4f625349cf9de7ead0547f47e77` and must not be merged wholesale.
  Its production-facing hero failed the visual gate: the desktop asset contains
  an `AI Generated` watermark and the mobile first screen is overloaded.
- Production received only the reviewed maintenance subset; there is no
  `src/` or `public/` diff from the prior live main.
- Workflow maintenance now pins official Node.js 24 actions:
  `actions/checkout@v7.0.1` and `actions/setup-node@v7.0.0`. Keep this change
  separate from future feature or visual work.

Recommended first message in a new chat:

```text
Проект Onixbit.
Папка: /home/aleksander/projects/client-systems/onixbit/site.
Прочитай PROJECT_STATE.md, ONIXBIT_SITE_ROADMAP.md и ONIXBIT_PUBLICATION_PLAN.md, затем сам проверь pwd и git status.
Работаем в React/Next из корня репозитория.
Используй onixbit-brand, ui-ux-pro-max, 21st.dev и motion, когда это уместно.
Общайся со мной на русском.
```

## Current Working Folder

Use the repository root:

```bash
/home/aleksander/projects/client-systems/onixbit/site
```

Do not use `onixbit-next`; the Next.js app was moved to the repository root.

## Stack

- Next.js 16.2.12
- React 19.2.4
- TypeScript
- Tailwind CSS 4 / PostCSS
- @fontsource Manrope + Montserrat for self-hosted open-source typography
- lucide-react for icons
- motion for React animations
- Playwright + axe for E2E and accessibility checks
- Lighthouse CI for local performance/SEO/accessibility audits
- Sentry-ready monitoring, disabled until `NEXT_PUBLIC_SENTRY_DSN` is configured
- Docker Compose + Caddy for production
- GitHub Actions for CI/deploy to FirstVDS

## Useful Commands

```bash
npm run dev
npm run check
npm run lint
npm run build
npm run browser:deps
npm run test:e2e
npm run test:e2e:built
npm run test:e2e:ui
npm run test:visual:update
npm run lhci
git status -sb
gh auth status -h github.com
```

`npm run check` runs lint, production audit, and build.

## Deployment

Repository remote:

```text
origin git@github.com:expertvizoff-cpu/onixbit.git
```

Main branch:

```text
main
```

Pushing to `main` can trigger GitHub Actions and the FirstVDS deployment workflow.

Deployment docs:

```text
DEPLOY_FIRSTVDS.md
```

## Current Important Commits

- `58c882c` - moved the Next.js app from `onixbit-next` to the repository root and removed old Tilda/VK/archive materials.
- `92b3b9d` - added the `motion` animation library.

## Recent Figma Work

- 2026-07-07: Created a separate Figma Page `36 Презентация / Техподдержка Битрикс24` in file `QmWZap5UbNRmBnLtvcshi7`.
- Presentation frames: `173:2` cover, `173:43` support areas, `173:83` request process, `173:134` PDF-reference tariffs, `173:183` typical tasks/cases, `173:223` contacts.
- Main link: `https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=173-2`.
- Tariff slide uses OCR-extracted values from `reference/рус Техническая поддержка Б24 презентация (1) (2).pdf`: `43 000 ₽`, `63 000 ₽`, `индивидуально`, `10 / 15 / от 25` hours, response times `120 / 90 / 60 / 30` minutes, additional hour `4 500 ₽`.

## Installed Local Codex Tools/Skills

These are local Codex environment notes, not project dependencies:

- `ui-ux-pro-max` installed at `/home/aleksander/.codex/skills/ui-ux-pro-max`.
- `onixbit-brand` installed at `/home/aleksander/.codex/skills/onixbit-brand`.
- Marketing, sales, SEO/GEO, React, TypeScript, frontend architecture, testing, accessibility, performance, visual-testing, and content skills are installed globally at `/home/aleksander/.codex/skills/`.
- `bitrix-aspro-photoconstructor` is installed globally for future Lifephotoshop/Bitrix/Aspro/photo-constructor work, but it is not an Onixbit project dependency.
- 21st.dev Magic MCP is configured locally in Codex config as `21st_magic`.
- VS Code Russian language pack was installed in the remote VS Code server.
- GitHub CLI `gh` installed at `/home/aleksander/.local/bin/gh`; run `gh auth login` when GitHub API/Actions access is needed.

Do not commit local API keys or Codex config files into this repository.

## Working Rules

- Critical production-freeze for the redesign, set by Aleksander on 2026-07-05: do not touch, replace, or publish over the working `onixbit.ru` production site until the new website is complete and Aleksander explicitly approves migration.
- This production-freeze overrides older auto-publish habits for all redesign work. Build in Figma, local files, safe preview routes, or explicit staging only.
- Always read `ONIXBIT_SITE_ROADMAP.md` and `ONIXBIT_PUBLICATION_PLAN.md` after this file. Current next step: create `/preview-onixbit-home-final` with the final brand and the first interactive request-route scene.
- Communicate with the user in Russian unless the user explicitly asks for another language.
- For website/UI work, use the root React/Next project.
- When Aleksander asks for website changes or visual/content edits, apply the changes directly to the site code in the same turn whenever feasible; do not stop at advice, a plan, or a description unless he explicitly asks for discussion only.
- Prefer existing local patterns before adding new abstractions.
- Use `onixbit-brand` for Onixbit-specific brand, copy, SEO, deployment, and handoff decisions.
- Use `ui-ux-pro-max` for UI/UX design, review, animation, layout, typography, color, accessibility, and responsive decisions.
- Use 21st.dev for UI inspiration/components/logos when helpful.
- Use `motion` sparingly for professional B2B motion: subtle reveals, state transitions, hover/tap feedback, and reduced-motion-friendly animations.
- Keep Onixbit business-focused: restrained, clear, credible, and conversion-oriented.
- When adapting Figma frames to production, keep the same site width rhythm as the working onixbit.ru site: use the existing `.ob-container` / `--ob-container` system (`1700px` desktop max, `calc(100% - 48px)` section gutters, header `calc(100% - 40px)`, and existing tablet/mobile gutter rules). Do not copy fixed 1440px Figma artboard coordinates as full-page production width.
- Public contacts: use `8 800 100-53-03` (`tel:+78001005303`) as the primary website/header phone. Keep `8 920 272-48-28` as the direct/mobile display phone in contacts and messengers, not as the main header phone. Use `tel:+79202724828` for the technical phone link when needed.
- For pricing, licenses, tariffs, certificates, and similar comparison blocks, preserve the desktop composition on common laptop widths (`1181-1440px`) with compact spacing instead of collapsing columns too early. Collapse to 2 columns only below the laptop range and to 1 column on mobile.
- When starting articles or case descriptions, remind Aleksander to create or provide `CONTENT_GUIDE.md`, `CASE_TEMPLATE.md`, and `TYPOGRAPHY_GUIDE.md`/examples so copy has a durable human editorial voice and factual case structure.
- Before finishing substantial work, run `npm run check` unless the change is docs-only or clearly does not affect code.
- Run `npm run test:e2e` after user-visible route, layout, form, or navigation changes.
- Run `npm run lhci` for performance, SEO, accessibility, or production-readiness audits.
- Publishing rule update 2026-07-05: the earlier auto-publish habit is suspended for redesign work. Do not push or deploy redesign changes to `main` or production until Aleksander explicitly approves the migration.
- For non-redesign maintenance that could affect the working `onixbit.ru` site, ask before publishing.

## Restart/Handoff Routine

If a session is interrupted without `/wrap`, the next Codex chat should restore context automatically:

1. Read `PROJECT_STATE.md`.
2. Read `ONIXBIT_SITE_ROADMAP.md`.
3. Read `ONIXBIT_PUBLICATION_PLAN.md`.
4. Confirm the production-freeze rule for redesign work.
5. Run `pwd`.
6. Run `git status -sb`.
7. Continue from the newest user request without asking the user to repeat these commands.
8. Speak Russian by default.

Before ending a substantial task, update this file if any of these changed:

- project structure
- dependencies
- deployment workflow
- active tools/skills
- important decisions
- last known next steps

Then run:

```bash
git status -sb
```

If code changed, run:

```bash
npm run check
```

## Current State

- 2026-07-29 prepared the accumulated website work for a safe GitHub backup on
  branch `astra/git-rollout-2026-07-29`. The public repository boundary was
  reviewed: local agent/browser/AgentMemory state, source reference files,
  server audit details, personal image material, bank requisites and generated
  QA/export folders are excluded from Git. The production redesign freeze
  remains active, so `main` and production deployment are intentionally
  untouched. Updated Next.js and eslint-config-next from `16.2.9` to `16.2.12`,
  PostCSS to `8.5.25`, and Sharp to `0.35.3`; `npm run check` reports zero
  production vulnerabilities and a successful build. Fixed the Zvukogram helper
  path guard for the canonical `onixbit/site` layout and made Playwright serial
  to prevent the local standalone server from being overloaded; the final E2E
  run passed all 44 tests.

- 2026-07-19 completed an explicitly approved infrastructure-only production
  change for the separate Bitrix24 media archive. The local and active Caddyfile
  now contain a dedicated `media` host that proxies to the private archive
  player over the existing website Docker network. The active Caddyfile was
  backed up before validation and graceful reload; the new certificate was
  issued, the media health endpoint returned HTTP 200, and the existing site
  remained HTTP 200. No redesign code, Bitrix24 data or S3 objects were changed.
  The local Caddyfile change is uncommitted and must be included in the next
  separately approved site release so deployment does not overwrite the route.

- 2026-07-07 created the first Figma A4 concept for the Onixbit technical support one-pager: `35 Техподдержка Ониксбит / A4 concept v1`, node `164:2` in the existing Figma file `QmWZap5UbNRmBnLtvcshi7` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=164-2`). Direction: document-style support sheet in the new Onixbit visual system, with a dark support-route hero, scope cards for Bitrix24 / 1C-Bitrix / 1C and exchanges, a request-processing timeline, support governance notes, and CTA contacts. Created local `reference/README.md`; Aleksander can upload competitor/reference PDFs there, preferably as `reference/support-example.pdf`. No production website route was changed.

- 2026-07-06 created separate Bitrix24 workspace at `/home/aleksander/projects/client-systems/onixbit/b24-cloud` with its own `AGENTS.md`, `PROJECT_STATE.md`, docs, `.env.example`, `.gitignore`, `package.json`, and read-only audit script `scripts/bitrix24-readonly-audit.mjs`. It has an independent Git repository on branch `main`; no remote and no initial commit yet. Use this project for future Bitrix24 portal work (`cloud` and `box`) instead of mixing those operations into the website repo. No real portal credentials connected and no portal changes performed.

- 2026-07-06 Bitrix24 portals work direction recorded in `ONIXBIT_BITRIX24_PORTALS_PLAN.md`. Aleksander has two portals: `cloud` and `box`. Preferred long-term connection model is OAuth / local app / scoped app access, with incoming webhooks only as a temporary limited option. Official Bitrix24 MCP is for current REST documentation, not for authorized access to the portals. Superseded by the separate workspace `/home/aleksander/projects/client-systems/onixbit/b24-cloud`; keep this website repo only for historical docs and handoff links. No real portal URLs, webhooks, tokens, refresh tokens, passwords, SSH keys, or client data should be written to chat, docs, or committed code.

- 2026-07-06 Aleksander paused website redesign work after rejecting the current full-new-homepage/animated-preview direction. Do not continue `/preview-onixbit-home-final` or production site redesign work until a new explicit request. Durable pause handoff: `ONIXBIT_SITE_WORK_PAUSED_2026-07-06.md`. Recommended future direction if resumed: improve the existing site block by block and page by page, not another full-site animated concept. Production-freeze remains active.

- 2026-07-06 strengthened `ONIXBIT_HOMEPAGE_TZ.md` after the TЗ audit. Added explicit stage gates before code (`P0 / storyboard-ready`, `P1 / figma-ready`, `P2 / code-ready`, `P3 / production-ready`), storyboard frame requirements, sanitized mock-data dictionary, section decision map, diagnosis-offer boundaries, Figma review frame package, implementation decomposition contract, performance/interaction budgets, analytics decision contract, security/form contract, and P0/P1/P2 acceptance priority levels. Production-freeze remains active: no working `onixbit.ru`, no production routes, no push/deploy until explicit migration approval.

- 2026-07-05 installed missing curated skills for the Onixbit wow-homepage workflow: `figma-generate-design`, `figma-implement-design`, and `playwright-interactive`. They were installed into `/home/aleksander/.codex/skills/`. Codex may need a restart to auto-discover them, but their `SKILL.md` files were read manually and `ONIXBIT_HOMEPAGE_TZ.md` now records how to use them: Figma/storyboard before complex hero code, Figma-to-Next parity workflow, and stronger persistent-browser QA inventory. No production site changes were made.

- 2026-07-05 improved `ONIXBIT_HOMEPAGE_TZ.md` with a skill-coverage and missing-expertise gate for the wow homepage. The spec now maps local skills to concrete outputs: brand compliance, art-direction decisions, H1/CTA and objection mapping, diagnostic value equation, SEO/GEO/schema plan, typed React/Next scenario config, analytics plan, security notes, and Playwright/accessibility/performance verification package. It also records what skills cannot replace: real proof assets, sanitized work screens, Aleksander approval of H1/CTA/wow moment, secure Bitrix24 integration settings outside chat, and optional Figma/storyboard approval before code. Production-freeze remains active.

- 2026-07-05 added a `Wow Result Addendum` to `ONIXBIT_HOMEPAGE_TZ.md`. The homepage spec now requires a signature memory hook where a chaotic request becomes a controlled route, before/after route drama, realistic or sanitized work-screen mockups, persona-specific reading cues, objection handling through the route, diagnosis as a tangible mini-product, trust-through-process signals, art-direction quality checks, and H1/CTA selection options. Acceptance criteria now explicitly require the wow moment, broken-to-controlled contrast, serious business interfaces, objection handling, and a tangible diagnostic offer.

- 2026-07-05 expanded `ONIXBIT_HOMEPAGE_TZ.md` from a strong homepage concept into a production-ready homepage specification. Added no-placeholder rules, final content inventory, proof source table, first-10-second hero timeline, interaction state model, mobile route requirements, real lead form/Bitrix24 handoff requirements, final SEO package, editorial QA, Aleksander confirmation checklist, production-ready definition, and migration boundary. Production-freeze remains active: build only in `/preview-onixbit-home-final` until explicit migration approval.

- 2026-07-05 created `ONIXBIT_HOMEPAGE_TZ.md` as the working creative/technical specification for Stage 2 `/preview-onixbit-home-final`. It locks the corrected direction: final Onixbit logo, approved firm colors from `ONIXBIT_BRANDBOOK.md`, light public homepage with a dominant dark process screen, no generic blue enterprise palette, no standard repeated card blocks, scenario-driven request-route animation, CRO/sales/SEO/GEO/accessibility/performance/analytics/Playwright gates, and production-freeze preserved.

- 2026-07-05 Aleksander approved `ONIXBIT_PRODUCTION_URL_INVENTORY.md`: preserve all 18 current sitemap URLs; do not plan special redirects from unknown old URLs, treating the future launch as new over the preserved current URLs plus approved new URLs; primary phone display stays `8 800 100-53-03`; direct phone display is `8 920 272-48-28` with technical `tel:+79202724828` when needed. Stage 1 inventory is complete; next practical step is Stage 2 `/preview-onixbit-home-final` without touching production `onixbit.ru`.

- 2026-07-05 rewrote `ONIXBIT_PRODUCTION_URL_INVENTORY.md` in Russian and added the `Что нужно сделать Александру` checklist. Historical note: it was a draft at creation time, then Aleksander approved it later the same day; use the newer approved entry above as the active rule.

- 2026-07-05 created the initial `ONIXBIT_PRODUCTION_URL_INVENTORY.md` draft. Read-only production checks found 18 URLs in `https://onixbit.ru/sitemap.xml`, all responding `200`; known local preview routes checked on production returned `404`; `/api/health` returned `200` and is recorded as a technical endpoint outside sitemap/navigation. Historical note: this draft was later approved by Aleksander with the decisions recorded above.

- 2026-07-05 created `ONIXBIT_PUBLICATION_PLAN.md` as the detailed plan from the current post-brandbook point to final publication. It defines production-freeze, publication meaning, approval gates, target site hierarchy/navigation, stages from current inventory and homepage preview through process pages, public pages, proof, conversion, SEO, QA, migration, publication, and post-launch stabilization. Future chats must read it after `PROJECT_STATE.md` and `ONIXBIT_SITE_ROADMAP.md`.

- 2026-07-05 created `ONIXBIT_SITE_ROADMAP.md` as the durable redesign plan and recorded Aleksander production-freeze decision: the working `onixbit.ru` site must not be touched, replaced, pushed over, or auto-deployed during redesign. Future chats must read this file after `PROJECT_STATE.md` and continue from `/preview-onixbit-home-final` unless Aleksander gives a newer instruction.

- 2026-07-05 finalized the Onixbit brandbook as v1.1. Updated `ONIXBIT_BRANDBOOK.md` from working source to approved source of truth, added final Figma board `34 Брендбук Ониксбит / финал` (`152:3`), and exported the board as `output/brandbook/onixbit-brandbook-final-figma.pdf`. Control screenshot saved to `output/screenshots/onixbit-brandbook-final-figma.png`. Created handoff files in `output/brandbook/`: `README.md`, `ONIXBIT_BRANDBOOK_FINAL.md`, `LOGO_PACKAGE_README.md`, and archive `onixbit-brandbook-final-package-2026-07-05.zip`. Validation: Figma board has 174 nodes, 103 text layers, 0 out-of-bounds nodes, 0 missing fonts; PDF byte check found `/Font` and `/ToUnicode`; `unzip -t output/brandbook/onixbit-brandbook-final-package-2026-07-05.zip` passed. Final rule: use `lockup/primary` as the master logo and treat earlier logo boards (`26`, `26A`, `ОС`, `ЗН`, `RU-05`, `BIT-R2`) as historical exploration only unless an asset is included in `output/logos/final/`.

- 2026-07-05 installed a local project-scoped `zip` tool without sudo by downloading `zip_3.0-13ubuntu0.2_amd64.deb` into `.work/tools/debs/` and extracting it to `.work/tools/zip/`. Used it to rebuild `output/requisites/onixbit-requisites-11.docx` and `output/requisites/onixbit-requisites-11-editable.docx` as true OpenXML Word documents with editable text/tables, replacing the earlier image-only main `.docx`. The old image-only file remains only as `output/requisites/onixbit-requisites-11-image-only.docx`. Validation: `unzip -t output/requisites/onixbit-requisites-11.docx` passed; text strings such as `ИП Тужилкин Александр Павлович`, `Расчетный счет`, and `info@onixbit.ru` are present in `word/document.xml`. PDF remains `output/requisites/onixbit-requisites-11.pdf` from Figma with `/Font` and `/ToUnicode` entries.

- 2026-07-05 corrected the requisites export after Aleksander rejected the image-only Word/PDF result. The current final PDF is `output/requisites/onixbit-requisites-11.pdf`, downloaded from Figma node `82:2` as a vector PDF; byte inspection confirmed `/Font` and `/ToUnicode` entries, so it is not the earlier flat PNG-in-PDF export. The Word-editable version is `output/requisites/onixbit-requisites-11.doc` plus alias `output/requisites/onixbit-requisites-11-editable.doc`; both are Word-compatible HTML documents with real text/tables, with source `output/requisites/onixbit-requisites-11-editable.html`. The old image-only DOCX was backed up as `output/requisites/onixbit-requisites-11-image-only.docx`; do not use it as the final Word file. A true `.docx` rebuild was blocked in the current sandbox because `zip` is unavailable and Python/Node write execution is blocked by the environment.

- 2026-07-05 exported the final Figma requisites variant `A4 / Requisites 11 Wow Compact Footer` (`82:2`) as downloadable local files in `output/requisites/`: `onixbit-requisites-11.pdf`, `onixbit-requisites-11.docx`, and source PNG `onixbit-requisites-11.png`. The PNG was rendered from Figma at `794 x 1123`; PDF is an A4 single-page file with the rendered design; DOCX is an A4 Word document containing the same full-page design image. Validation: file sizes checked; `unzip -t output/requisites/onixbit-requisites-11.docx` passed with no errors.


- 2026-07-05 updated the Figma requisites sheet `11 Requisites / PDF options` (`35:2`) to use the new final Onixbit logo system. Replaced 11 old raster/PDF logo placeholders named `Logo / real header PNG` / `Logo / real footer PNG` with vector clones from the final package: light layouts use `Logo / Onixbit final lockup primary`, dark header layouts use `Logo / Onixbit final lockup inverse-red`. Also replaced 2 small footer marks `Footer / Onixbit mark PNG` with `Logo mark / Onixbit final red`. Validation: old logo count `0`, new logo count `13`; control screenshot from Figma visually checked that QR codes and text were not affected.


- 2026-07-05 занёс финальную систему логотипа Onixbit в Figma отдельным листом `33 Логотип Ониксбит / финальный пакет`. Борд `143:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=143-3`) содержит 8 векторных экспортных ассетов `444 x 110`: `lockup` и `mark-as-o` в режимах `primary`, `inverse-red`, `inverse-white`, `mono-dark`. Имена экспортных фреймов: `ASSET / lockup / ... / 444x110` и `ASSET / mark-as-o / ... / 444x110`. Проверка Figma metadata подтвердила структуру и размеры; inline screenshot через MCP визуально проверен: варианты не обрезаны, белые версии читаются на тёмных карточках. Правило использования на борде: `lockup/primary` — основной мастер, `mark-as-o` — осознанная альтернативная версия.


- 2026-07-05 finalized the Onixbit logo package into two approved structures as requested by Aleksander: `lockup` (separate sign + full `ОНИКСБИТ`) and `mark-as-o` (sign replaces the first `О`). Final assets live in `output/logos/final/` with subfolders `lockup/`, `mark-as-o/`, and `previews/`. Each structure has four transparent `444 x 110` SVG/PNG modes: `primary`, `inverse-red`, `inverse-white`, and `mono-dark`. Recommendation recorded in `output/logos/final/README.md` and `ONIXBIT_BRANDBOOK.md`: use `lockup/primary` as the default master; use `mark-as-o` only as a deliberate alternate. Preview sheet: `output/logos/final/previews/onixbit-final-logo-system-preview.png`. Archive: `output/logos/onixbit-final-logo-package-2026-07-05.tar.gz` (`zip` was unavailable in the environment). Verification: all PNGs are RGBA with real alpha; lockup visible bbox `386 x 49`, mark-as-o visible bbox `392 x 50`; all SVGs have no visible background rect.

- 2026-07-05 added the missing fourth `mark-as-O` color variant requested by Aleksander: red sign as the first `О`, dark `НИКС`, and red `БИТ`. Files: `output/logos/onixbit-bit-r2-mark-as-o-dark-onix-red-bit-444x110.png` and `.svg`. Colors are sign `#ED1C24`, `НИКС` `#0F172A`, and `БИТ` `#B80F17`. Verification: transparent `444 x 110` PNG, RGBA, `hasAlpha: true`, visible bbox `392 x 50` at `x=26,y=30`, alpha min `0`, max `255`; SVG has no visible background rect. Updated preview sheet: `output/screenshots/onixbit-logo-mark-as-o-variants-preview.png`.

- 2026-07-05 added three `mark-as-O` logo variants where the abstract sign replaces the first Cyrillic `О` and the remaining word starts from `НИКСБИТ`. Files: `output/logos/onixbit-bit-r2-mark-as-o-all-white-444x110.png` / `.svg`, `output/logos/onixbit-bit-r2-mark-as-o-white-red-444x110.png` / `.svg`, and `output/logos/onixbit-bit-r2-mark-as-o-white-dark-444x110.png` / `.svg`. Geometry is shared across all three: transparent `444 x 110` PNG, visible bbox `392 x 50` at `x=26,y=30`, RGBA with alpha min `0` and max `255`. Color logic: all-white makes sign and all letters white; white-red uses red sign `#ED1C24`, white `НИКС`, dark-red `БИТ` `#B80F17`; white-dark uses dark sign and `БИТ` `#0F172A`, white `НИКС`. Preview sheet saved to `output/screenshots/onixbit-logo-mark-as-o-variants-preview.png`.

- 2026-07-05 added two dark-background variants from the clean vector no-background `BIT-R2` master. Files: `output/logos/onixbit-bit-r2-vector-no-bg-onix-white-444x110.png` / `.svg` where only the `ОНИКС` word part is white while the sign remains `#ED1C24` and `БИТ` remains `#B80F17`; and `output/logos/onixbit-bit-r2-vector-no-bg-all-white-444x110.png` / `.svg` where the sign and all word letters are white. Both PNGs are `444 x 110`, RGBA, `hasAlpha: true`, with the same transparent canvas alpha stats as the vector master: transparent `39,806`, partial `1,822`, opaque `7,212`. Both SVGs have no visible background rect.

- 2026-07-05 corrected the Bitrix24 no-background logo export after Aleksander clarified that the source vector logo should be exported without adding any background, not post-processed from a raster/plate variant. Created Figma export frame `ASSET / BIT-R2 vector no-bg / 444x110` (`140:2`) from the original vector logo node `129:31` (`Лого BIT-R2`). The frame is exactly `444 x 110`, has `fillsCount: 0`, and contains a scaled clone of the vector logo (`391.6 x 57.1`, centered at `26,26`). Final local files: `output/logos/onixbit-bit-r2-vector-no-bg-444x110.png` and `output/logos/onixbit-bit-r2-vector-no-bg-444x110.svg`. Verification: PNG is `444 x 110`, `8-bit/color RGBA`, non-interlaced, `hasAlpha: true`; alpha stats are min `0`, max `255`, transparent `39,806`, partial `1,822`, opaque `7,212`. The SVG was cleaned of the MCP/Figma service background rect `#F5F5F5`; it keeps only the logo paths and a non-rendering clipPath. Recommendation: use `onixbit-bit-r2-vector-no-bg-444x110.png` for Bitrix24 upload. The earlier `onixbit-bit-r2-no-bg-container-444x110.png` is superseded for this request.

- 2026-07-05 created the simple no-background Bitrix24 upload logo requested by Aleksander after the glass/clear variants. Final file: `output/logos/onixbit-bit-r2-no-bg-container-444x110.png`. It is a transparent `444 x 110` PNG built from the verified `CLEAR-H1` logo, scaled to a visible logo bbox of `382 x 58` and centered at offset `31,26` so it uses the Bitrix24 container more fully without touching the edges. Alpha stats from generation: 30,581 transparent pixels, 1,552 partial pixels, and 16,707 opaque pixels. Superseded: do not use this as the main no-background export after the vector-source correction; use `output/logos/onixbit-bit-r2-vector-no-bg-444x110.png` instead.

- 2026-07-05 added true-transparent glass-plate Bitrix24 logo variants after Aleksander asked whether the glass versions could also be made without a background. Created local upload-ready files `output/logos/onixbit-bit-r2-glass-g1-transparent-444x110.png` and `output/logos/onixbit-bit-r2-glass-g2-transparent-444x110.png` from the previous glass renders by removing only the service background color `#F6F8FB` and converting the glass surface to partial alpha. Verification: both are `444 x 110`, RGBA, non-interlaced. `glass-g1-transparent` has 11,251 fully transparent pixels, 33,553 partially transparent glass pixels, and 4,036 opaque logo/text pixels. `glass-g2-transparent` has 13,534 fully transparent pixels, 31,464 partially transparent glass pixels, and 3,842 opaque logo/text pixels. For Bitrix24 upload, prefer the filenames ending in `-transparent-444x110.png`; the older `glass-g1-444x110.png` / `glass-g2-444x110.png` are still opaque and should not be used for the portal logo.

- 2026-07-05 fixed the Bitrix24 logo transparency issue after Aleksander uploaded the previous PNG and saw a solid white/background rectangle. The cause was confirmed locally: `GLASS-G1` and `CARBON-C1` were RGBA files but their alpha channel was fully opaque (`alpha min/max 255 255`). Created Figma page `32 Логотип Ониксбит / прозрачный PNG без фона`, board `138:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=138-3`) with no-plate contour variants `CLEAR-H1`, `CLEAR-H2`, and `CLEAR-D1`. Because Figma/MCP raw export still composited transparent areas against the board color `#F6F8FB`, local post-processing replaced only that service background color with alpha `0`. Upload-ready transparent files are `output/logos/onixbit-bit-r2-clear-h1-444x110.png`, `output/logos/onixbit-bit-r2-clear-h2-444x110.png`, and `output/logos/onixbit-bit-r2-clear-d1-444x110.png`; use the files WITHOUT `.raw` in the filename. Verification: all three are `444 x 110`, RGBA, non-interlaced; `CLEAR-H1` has 40,109 transparent pixels out of 48,840. Recommendation: upload `CLEAR-H1` first, then `CLEAR-D1` if the portal background is very dark/colorful. Control sheet saved to `output/screenshots/onixbit-logo-bit-r2-clear-transparent-check.png`.

- 2026-07-05 finalized the Bitrix24 portal PNG export variants for `BIT-R2` after the earlier white-plate version felt too rough. Page `31 Логотип Ониксбит / BIT-R2 PNG 444x110`, board `134:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=134-3`) now contains six transparent-background PNG candidates: `GLASS-G1`, `GLASS-G2`, `CARBON-C1`, `EDGE-E1`, `PAPER-P1`, and `RED-R1`. Export frames `134:15`, `134:29`, `134:43`, `134:57`, `134:71`, and `134:84` were corrected to `contentsOnly: false`, `clipsContent: true`, and exact `444 x 110` size, with softened internal shadows so effects do not expand the PNG bbox. Local upload-ready files saved in `output/logos/`: `onixbit-bit-r2-glass-g1-444x110.png`, `onixbit-bit-r2-glass-g2-444x110.png`, `onixbit-bit-r2-carbon-c1-444x110.png`, `onixbit-bit-r2-edge-e1-444x110.png`, `onixbit-bit-r2-paper-p1-444x110.png`, and `onixbit-bit-r2-red-r1-444x110.png`. Control sheet saved to `output/screenshots/onixbit-logo-bit-r2-portal-png-444x110.png`. Recommendation: try `GLASS-G1` first for the Bitrix24 portal because it keeps the logo serious and universal without the crude white-plate feel; keep `CARBON-C1` as the stronger dark premium alternative. Local `file` verification confirmed all six PNGs are `444 x 110`, RGBA, non-interlaced.

- 2026-07-05 created the Bitrix24 portal-specific universal background test for `BIT-R2`. Page `30 Логотип Ониксбит / BIT-R2 универсальный фон`, board `130:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=130-3`) tests one-logo use on arbitrary employee-selected portal backgrounds. It compares transparent `BIT-R2`, protected white plate `U1`, compact white plate `U2`, and white halo `U3` across white, black, Bitrix blue, red, gray, and photo-like backgrounds. Recommendation: for Bitrix24 upload use `U1`, a single PNG/SVG with transparent outside area and an internal white logo plate, because bare transparent text fails on dark/red backgrounds and halo looks less premium. Export node `130:51` (`ASSET / BIT-R2 universal / transparent outside`) was saved locally as `output/logos/onixbit-bit-r2-universal-portal.png`; control sheet saved to `output/screenshots/onixbit-logo-bit-r2-universal-background.png`.

- 2026-07-05 created a separate Figma test sheet for coloring the `БИТ` part of the `ОНИКСБИТ` wordmark to match the sign color. Page `29 Логотип Ониксбит / БИТ в цвете`, board `129:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=129-3`) compares `CTRL RED` (red sign + black word), `BIT-R1` (red sign + bright red `БИТ`), `BIT-R2` (red sign + deep red `БИТ`), `BIT-B1` (blue sign + blue `БИТ`), `BIT-M1` (black sign + red `БИТ`), and `BIT-G1` (gradient sign + red `БИТ`). It includes candidate cards, primary recommendation notes, website-header checks, dark-background checks, and compact/mobile checks. Current read: `BIT-B1` is the calmest/most enterprise version, `BIT-R2` is the best red firm-color test, while `BIT-R1` and `BIT-G1` are stronger but split the word more and should stay as bold/promo options rather than the master. Control PNG saved to `output/screenshots/onixbit-logo-colored-bit-test.png`.

- 2026-07-05 created the `ОС-02` color and usage-rules logo sheet in Figma after Aleksander approved moving forward and asked to also test the firm red icon and a gradient icon. Page `28 Логотип Ониксбит / ОС-02 цвет и правила`, board `128:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=128-3`) contains five practical variants: `01 MASTER` mono (`#0F172A`), `02 ACCENT` blue sign + black word (`#0369A1` + `#0F172A`), `03 INVERSE` white on dark, `04 RED` firm red sign + black word (`#ED1C24` + `#0F172A`), and `05 GRADIENT` red/deep-red/yellow sign + black word (`#B80F17` -> `#ED1C24` -> `#FFD45A`). It also includes palette tokens, website-header checks, dark-background checks, favicon/small-size checks, and misuse guidance. Current recommendation: keep mono as the master, blue-accent as the calm primary color version, red as the firm-color edition, and gradient as a large-format/promo-only variant. Control PNG saved to `output/screenshots/onixbit-logo-os02-color-rules.png`.

- 2026-07-05 created the final practical logo application check in Figma after Aleksander accepted the direction and confirmed the experimental branch should use `ЗН-02`/`ЗН-03`. Page `27 Логотип Ониксбит / финальная проверка`, board `124:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=124-3`) compares five candidates only: `ОС-01`, `ОС-02`, `ОС-03`, `ЗН-02`, and `ЗН-03`. The board tests candidate cards, desktop header, mobile header, favicon/small-size mark, dark inversion, and document/KP usage. Current recommendation: `ОС-02` is the calm primary candidate because it keeps the `О` ordinary and the wordmark whole; `ОС-03` is the stronger reserve if the header needs more weight; `ЗН-02` remains an experiment because using the sign as the first `О` is interesting but raises reading risk. Control PNG saved to `output/screenshots/onixbit-logo-final-application-check.png`.

- 2026-07-05 corrected the RU-05 wordmark direction after Aleksander rejected the accented standalone round `О`. Created Figma page `26 Логотип Ониксбит / 05 без акцента и знак-О`, board `120:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=120-3`) with two branches: `ОС-01`-`ОС-06` keep the basis-05 full uppercase Cyrillic `ОНИКСБИТ` wordmark with the first `О` as a normal letter, and `ЗН-01`-`ЗН-06` test the selected `26A-M1` sign as the first `О` followed by `НИКСБИТ`. Current recommendation: branch A is closer to Aleksander feedback, especially `ОС-01`, `ОС-02`, `ОС-03`; branch B should be judged only as an experiment, with `ЗН-02` and `ЗН-03` the most collected. Boundary for next pass: no separate circular `О` accent/ring; if using the sign as `О`, it must start the word rather than sit as an extra left-side logo. Control PNG saved to `output/screenshots/onixbit-logo-05-normal-o-and-mark-as-o.png`.

- 2026-07-05 developed the liked `RU-05`/basis-05 Russian wordmark direction after Aleksander clarified that the first Cyrillic `О` must be round. Created Figma page `25 Логотип Ониксбит / RU-05 круглая О`, board `119:2` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=119-2`) with 12 uppercase Cyrillic `ОНИКСБИТ` variants built from the selected `26A-M1` sign and a custom vector circular `О` (stroked ellipse, width = height) paired with the heavy `НИКСБИТ` wordmark. Current strongest directions: `КР-01`, `КР-05`, and `КР-10`; `КР-11` is only an edge-case thickness test. Boundary for next pass: keep logo text Russian-only uppercase, keep the first `О` circular, and avoid making the `О` feel like a separate decorative icon. Control PNG saved to `output/screenshots/onixbit-logo-ru05-round-o-wordmark.png`.

- 2026-07-04 created the Russian-only uppercase wordmark exploration in Figma after Aleksander requested variants only in Russian and in one register. Page `24 Логотип Ониксбит / русский wordmark`, board `115:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=115-3`) uses only `ОНИКСБИТ` in upper-case Cyrillic, with the selected `26A-M1` mark. It contains 12 lockup variants, header checks, inverse hero check, and font/spacing notes. Current shortlist: `RU-01` and `RU-12`; keep `RU-02` as the heavier status reserve. Boundary for next pass: no Latin `Onixbit`, no `onixbit.ru`, no mixed `Ониксбит/ОНИКСБИТ` casing on this wordmark board. Control PNG saved to `output/screenshots/onixbit-logo-russian-uppercase-wordmark.png`.

- 2026-07-04 further developed the selected Onixbit `26A` logo direction in Figma. Created page `22 Логотип Ониксбит / 26A мастер-доводка`, board `113:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=113-3`) with `26A-M1`-`26A-M8`, master lockups, optical grid/clear-space check, wordmark checks, inverse/app/header/document tests. Current recommendation: keep developing `26A-M1` as the primary selected mark, with `26A-M2` as the calmer reserve. Control PNG saved to `output/screenshots/onixbit-logo-26a-master-refinement.png`. Also created page `23 Логотип Ониксбит / новые идеи значка`, board `114:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=114-3`) with 36 fresh icon ideas. Early interest list: `N2-02`, `N2-04`, `N2-06`, `N2-09`, `N2-14`, `N2-17`, `N2-23`, `N2-28`, `N2-36`. Control PNG saved to `output/screenshots/onixbit-logo-fresh-icon-ideas-02.png`.

- 2026-07-04 continued the accepted `26A` logo direction in Figma after Aleksander clarified that the mark must stay abstract, not a Cyrillic `Б`. Created page `20 Логотип Ониксбит / 26A доводка`, board `109:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=109-3`) with six polished variants `26A-R1`-`26A-R6`, lockup checks, inverse checks, favicon/app/header/document tests. Current recommendation: `26A-R1` as the primary candidate and `26A-R2` as the calmer reserve. Control PNG saved to `output/screenshots/onixbit-logo-26a-refined-system.png`. Added a separate first-level exploration page `21 Логотип Ониксбит / значки первого уровня`, board `110:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=110-3`) with 20 abstract system-loop silhouettes based on the same idea. Shortlist for further development: `L1-01`, `L1-02`, `L1-06`, `L1-08`, `L1-11`, `L1-16`, `L1-20`. Control PNG saved to `output/screenshots/onixbit-logo-26a-first-level-icons.png`. Boundary: no literal `Б`, no internal crossbar, no red partner spiral, no mascot/character feel.

- 2026-07-04 logo direction correction after Aleksander rejected literal `Б`: stop developing F1/`Б-монолит` as the main logo. The preferred direction is now `26`/`26A` as an abstract loop/system contour, not a letter mark. Created Figma page `19 Логотип Ониксбит / развитие 26A`, board `107:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=107-3`) with polished 26A variants and an explicit rule: no literal `Б`, no internal crossbars, no red circle/partner spiral, no character-like mark. Current shortlist: `26A-1`, `26A-8`, original `26A`. Local screenshot saved to `output/screenshots/onixbit-logo-26a-abstract-loop-development.png`.

- Historical/superseded note from the earlier logo pass: after Aleksander selected silhouette 26, it was briefly interpreted as a rotated/looped `Б` with a Bitrix-ecosystem association. This literal-letter framing was later rejected. Keep only the useful part: the abstract 26/26A system-loop silhouette, custom black-and-white geometry, and distance from the 1C-Bitrix red circular/concentric logo.
- 2026-07-04 `Б-loop` refinement in Figma: created page `17 Логотип Ониксбит / Б-loop refinement`, board `104:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=104-3`) with 12 variants `26A`-`26L`, inverse checks, and 16/32/64 px tests. Shortlist from that pass: `26B`, `26C`, `26H`, `26I`; local screenshot saved to `output/screenshots/onixbit-logo-26-b-loop-refinement.png`.
- 2026-07-04 focused logo finalists in Figma: created page `18 Логотип Ониксбит / 26 финалисты`, board `105:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=105-3`) with a narrower serious set based on 26. Current recommendation: `F1 / Б-монолит` as the primary candidate because it is status-focused, readable at favicon size, and farther from the 1C-Bitrix circular spiral; `F2 / Б-loop 26` is the reserve if Aleksander wants to preserve more of the original 26 motion. Local screenshot saved to `output/screenshots/onixbit-logo-26-finalists.png`.

- 2026-07-04 icon silhouette pass for the Onixbit logo: after agreeing that the previous mark still felt weak, created a new Figma page `16 Логотип Ониксбит / силуэты знака` in file `QmWZap5UbNRmBnLtvcshi7`, board `101:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=101-3`). This is intentionally not a final logo board: it contains 30 black-and-white icon silhouettes without color or wordmark, each tested at 16/32/64 px, plus a raw shortlist for manual refinement: 01, 03, 07, 11, 13, 16, 20, 24, 27. Purpose: filter for strong mass, negative space, and serious B2B feel before drawing a polished mark. Control PNG saved to `output/screenshots/onixbit-logo-icon-silhouettes.png`.

- 2026-07-04 logo redesign follow-up after Aleksander rejected the Pac-Man/character-like feel: added a new Figma page `15 Логотип Ониксбит / серьёзные варианты` in file `QmWZap5UbNRmBnLtvcshi7`, board `99:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=99-3`). The board contains two stronger B2B logo directions plus a small reserve idea: A `Управляющий контур` was made more monolithic and less character-like, B `Системный модуль` is the current recommended main direction because it reads drier, more technological, and better communicates the site-CRM-1C system route. It includes lockups, mono/inverted/app/header/document checks, selection criteria, and next-step notes for manual wordmark polishing and trademark search. Control PNG saved to `output/screenshots/onixbit-logo-serious-candidates.png`.

- 2026-07-04 logo/trademark exploration: created a new separate Figma page `14 Логотип Ониксбит / товарный знак` in file `QmWZap5UbNRmBnLtvcshi7`, board `94:3` (`https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=94-3`). Direction: `Контур события` - custom vector Cyrillic wordmark `ОНИКСБИТ` plus a distinct operational-contour mark that avoids partner logos and generic CRM/API/1C labels, so it is cleaner as a future trademark candidate. The board includes rationale from Onixbit positioning, large-tech brand-system references, application checks, color/mono/inverted versions, and trademark-readiness notes. Control PNG saved to `output/screenshots/onixbit-logo-trademark-system.png`. Installed local Figma-related skills for future sessions: `figma-use`, `figma-generate-library`, and `figma-create-design-system-rules`; restart Codex for automatic skill discovery, but they were read/applied manually during this session.
- 2026-07-04 brandbook v1: added durable source `ONIXBIT_BRANDBOOK.md`, product-marketing context `.agents/product-marketing.md`, and the internal preview route `/preview-onixbit-brandbook` (`src/app/preview-onixbit-brandbook/`). Added live CSS variables for process/signal colors and spacing in `src/app/globals.css` so Figma token code syntax points to real `var(--ob-...)` names. The current Figma file `https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=0-1&t=fGCffiUH9YpQMKjg-1` now has page `10 Brandbook`, root frame `21:3`, and variable collection `Onixbit / Brand Tokens` with 30 variables (16 COLOR, 12 FLOAT, 2 STRING), scoped and with WEB code syntax. Figma visual QA fixed 1px auto-layout sections and text overlap; final screenshot check showed clean sections. Checks passed: `npm run lint`, `npm run build`, Figma MCP screenshot QA, and Playwright production audit on `http://127.0.0.1:3118/preview-onixbit-brandbook` with desktop/mobile status 200, document horizontal overflow 0, and mobile bounding-box offender count 0. Screenshots saved to `output/playwright/onixbit-brandbook-desktop.png` and `output/playwright/onixbit-brandbook-mobile.png`. Note: `next dev` on port 3107 failed because the environment hit the OS file watch limit while watching Sentry/Next node_modules; production `next start` worked.
- 2026-07-04 handoff before new chat: Codex config was updated at `/home/aleksander/.codex/config.toml` with `sandbox_mode = "danger-full-access"` to stop the recurring `apply_patch` / sandbox `bwrap: loopback: Failed RTM_NEWADDR` failures. Backup: `/home/aleksander/.codex/config.toml.bak-sandbox-20260704`. `codex doctor` now reports `unrestricted fs + enabled network`; the current already-running chat still uses the old runner, so open a fresh Codex chat for the fix to take effect.
- 2026-07-04 width rule after Figma MCP generation: Aleksander clarified that the redesign must preserve the working onixbit.ru site width. Production implementation should keep the existing `--ob-container: 1700px` rhythm and `.ob-container` gutters instead of treating the 1440px Figma frames as the page width.
- 2026-07-04 Figma MCP generation complete: the current Figma file `https://www.figma.com/design/QmWZap5UbNRmBnLtvcshi7/Untitled?node-id=0-1&t=fGCffiUH9YpQMKjg-1` was updated through the official Figma MCP `use_figma` tool. The file now has real working pages `00 Cover`, `01 Styles`, `02 Site Map`, `03 Light Pages`, `04 Process Pages`, `05 Key Screens`, `06 Homepage Film`, `07 Services`, `08 Articles`, and `09 Components`. Generated content includes local Onixbit paint/text styles, the hybrid sitemap board, 13 light page wireframes, 6 dark process-page wireframes, 3 key concept screens plus review notes, 5 desktop and 5 mobile homepage film storyboard frames, service route concept, article diagnostic concept, and a HUD component kit. Figma MCP validation confirmed 40 top-level generated frames and no duplicate frame names; screenshots were generated for sitemap frame `16:104`, key homepage frame `16:967`, and key request-route frame `16:1120`. Fonts resolved to Montserrat SemiBold and Manrope Regular/SemiBold. One stale duplicate sitemap frame from an earlier generator run was removed.
- 2026-07-04 site architecture prototype: added durable full-site architecture document `ONIXBIT_SITE_ARCHITECTURE.md` and working preview route `/preview-onixbit-architecture`. It maps the hybrid site into light public pages, dark process-demo pages, proof/content pages, contacts/legal pages, header/footer navigation, page block plans, internal linking, visual style rules, and implementation order. The live prototype lets Aleksander switch page groups, inspect each page URL, role, audience, CTA, block list, and outgoing links. Fresh preview: `http://185.125.103.25:3114/preview-onixbit-architecture`. Checks passed: `npm run lint` with only existing Figma plugin warnings, `npm run build`, and custom Playwright desktop/tablet/mobile checks with no console errors, no horizontal overflow, 5 page-group tabs, 6 process buttons, and active `/processy/marshrut-zayavki` detail rendering correctly.
- 2026-07-04 Figma workflow update: `figma/onixbit-setup/code.js` now generates a Figma redesign board from the approved architecture. The plugin creates pages `02 Site Map`, `03 Light Pages`, `04 Process Pages`, and `05 Key Screens`, draws a hybrid sitemap, light wireframes for all public/support pages, dark wireframes for all process-demo pages, and three first visual concept screens: homepage, Bitrix24 service page, and request-route process page. `figma/onixbit-setup/README.md` documents running the local development plugin from `figma/onixbit-setup/manifest.json`. Checks passed: `node --check figma/onixbit-setup/code.js` and `npm run lint` with no warnings.
- 2026-07-04 Figma MCP setup: the old bearer-token Figma MCP config was removed and re-added using the official OAuth flow with `codex mcp add figma --url https://mcp.figma.com/mcp`. `codex mcp list` now shows `figma` enabled with `Auth OAuth`. The current active chat did not hot-load the new Figma tools, so the next step is to restart Codex/open a fresh chat, then verify that Figma tools are available and test them on the current Figma file link.
- 2026-07-04 hybrid redesign architecture decision: Aleksander proposed mixing two site modes, and this is now the preferred direction. Keep the main public site mostly light, readable, fast, SEO-friendly, and trust-building; move dark cinematic animation into separate business-process pages such as request route, website order through CRM/1C, exchange failure, support, and executive control. No mandatory preload screen before useful content; process pages can be more immersive but must always link back to the matching service, proof/cases, and contact form. A durable reference list and evaluation workflow lives in `ONIXBIT_REFERENCE_SITES.md`.
- 2026-07-03 signature redesign idea: the scroll animation should demonstrate real client scenarios, especially the path of a request from website/form/channel into Bitrix24 CRM, manager tasks/robots, 1C data exchange, analytics/control, and support. This is now the main business reason for the advanced animation: make the value of connecting site + Bitrix24 + 1C obvious to a client. Aleksander added references Twinbru, Species in Pieces, and KKL Luzern Experience; Stripe is rejected as chaotic.
- 2026-07-03 detailed interactive scene pattern: combine the macro scroll route with micro demos for individual system parts. Preferred pattern is foreground action -> background system state changes immediately, for example a website chat widget in front of a Bitrix24 CRM funnel: visitor types a message, then a new lead/deal appears in the CRM board and assignment/tasks/robots are highlighted.
- 2026-07-03 system-tour prototype at `/preview-onixbit-system-tour`. It is a standalone preview route with self-made HTML/SVG/CSS demo UI: website chat widget in foreground, Bitrix24-style CRM funnel in background, new deal creation, manager assignment, robot task, 1C data exchange, and control/analytics highlights. Second iteration added a switchable micro-scene lab for four scenarios: chat request, website order through 1C/CRM, CRM robot/task, and support request. Checks passed: `npm run lint` with only existing Figma plugin warnings, `npm run build`, and Playwright desktop/tablet/mobile scenario switching checks with no horizontal overflow and no console errors. Fresh preview server: `http://185.125.103.25:3112/preview-onixbit-system-tour`. Follow-up fix: micro-scene action buttons are real demo controls now; clicking them changes the button state to `Действие ушло в системы`, highlights system updates, and announces the accepted demo event. Third iteration added the `сценарий будущей главной` storyboard section with four chapters and a production-materials panel; Playwright verified this section on desktop/tablet/mobile with 4 timeline items, no horizontal overflow, and no console errors. Fourth iteration added a clickable `режиссура реальных материалов` production-studio block with five future real scenes: incoming request, manager assignment, order and 1C, exchange failure, and executive control. Each scene records human action, system reaction, business outcome, materials to collect, and proof message; Playwright verified switching on desktop/tablet/mobile with no horizontal overflow and no console errors. Fifth iteration added a `первые сцены оживляем` live-cut block with three stronger interactive fragments: request not lost, order through 1C, and exchange failure before complaint. Each fragment has foreground action, route nodes, immediate system reactions, metrics, and a clear proof statement; Playwright verified tab switching and responsive geometry on desktop/tablet/mobile. Sixth iteration added a focused `Заявка с сайта проходит весь маршрут` scene inside live-cut: a 5-step interactive route from Chat to CRM, Manager, 1C, and Control, with stepper, next-step control, website panel, system mini-board, updates, and outcome. Checks passed: `npm run lint`, `npm run build`, and Playwright desktop/tablet/mobile request-focus checks with no horizontal overflow and no console errors. Seventh iteration polished this focus scene into a more sales-ready demonstration: each step now shows the client action, the automation, and the management-control meaning; the website widget has a live event tag; the scene ends with proof metrics. Checks passed: `npm run lint`, `npm run build`, and fresh Playwright desktop/tablet/mobile checks on port 3113 with zero page overflow, zero internal overflow, no console errors, 3 director cards, and 3 proof items. Eighth iteration added a visible request journey map: horizontal on desktop/tablet and vertical on mobile, with animated fill, pulse, active nodes, and a short caption that explains the request has one traceable route across systems. Checks passed again: `npm run lint`, `npm run build`, and Playwright desktop/tablet/mobile route-map checks on port 3113 with 5 route nodes, 1 active node, zero page overflow, zero internal overflow, and no console errors. Ninth iteration added a visible `гибридная архитектура` block: light main website for trust/SEO/conversion, dark process pages for immersive demos, and a navigation bridge back to services, proof, and forms. Checks passed: `npm run lint`, `npm run build`, and Playwright desktop/tablet/mobile hybrid-block checks on port 3113 with 3 architecture cards, 3 route cells, light/dark visual contrast, zero page overflow, zero internal overflow, and no console errors.
- 2026-07-03 homepage robot hero video reference: Aleksander placed a video reference in `output/robot-animation-reference/video_7784789b-c524-44d3-9675-7996af630de6.mp4`; it was copied to `public/media/home/onixbit-robot-hero-reference.mp4` and is now used in the homepage hero instead of the static robot image. `src/components/HomeHero.tsx` renders two muted looping autoplay video layers: a blurred enlarged backdrop (`object-fit: cover`) and a main foreground layer (`object-fit: contain`) with no controls, border, or outline; CSS uses radial masks to soften the rectangle edges so the video does not read as a framed card; after user feedback, the blurred backdrop layer is centered and scaled to 1.42x the frame so its edge is not visibly cropped. Fresh preview is `http://185.125.103.25:3108`. Checks passed: `npm run lint`, `npm run build`, and Playwright DOM/style check confirming videos=2, readyState=4, paused=false, controls=false, borderWidth=0px, backdrop blur, source `/media/home/onixbit-robot-hero-reference.mp4`, and backdrop size ratio 1.42x by 1.42x versus the frame.
- 2026-07-03 homepage robot hero rollback/fix: after Aleksander rejected the layered robot animation and the spoiled transparent cutout, the homepage hero now uses a newly regenerated background cutout from the saved original-with-background image: `public/media/home/onixbit-robot-dashboard-hero-bgcut-v2.png`. Background removal was done by edge-connected white-background flood fill, not by the earlier shape mask; alpha spot checks showed background edges alpha 0 while robot/head/body/panels/platform stayed alpha 255. `src/components/HomeHero.tsx` renders one Next/Image only; all robot motion layers, hand-press markers, panel glows, and `ob-robot-*` animation keyframes were removed from active JSX/CSS. Fresh preview is `http://185.125.103.25:3108`. Checks passed: `npm run lint`, `npm run build`, and Playwright DOM check confirming images=1, motionLayers=0, handPresses=0, panelGlows=0, and the `bgcut-v2` PNG as the image source. Next animation attempt should not be built from one flattened PNG; first prepare a precise prompt/storyboard and either generate separate transparent layer assets or use a real vector/canvas/3D rig.
- 2026-07-03 homepage robot hero refinement, second iteration: after Aleksander rejected the SVG/line-arm animation and incomplete cutout, the old armature classes/components were removed completely. The approved robot-dashboard artwork is preserved, and new transparent assets were generated from it: full no-background image `public/media/home/onixbit-robot-dashboard-hero-transparent.png`, static scene/panels layer `public/media/home/onixbit-robot-dashboard-scene.png`, and robot-only layer `public/media/home/onixbit-robot-dashboard-robot.png`. `src/components/HomeHero.tsx` now uses Next/Image for the static scene plus four clipped robot motion layers (body, head, left arm, right arm), four hand-press markers, and four edge-only panel glows; there are no connecting strips/lines. CSS timing now sequences left lower -> left upper -> right upper -> right lower with body turn first, head turn lagging, arm/touch, then panel-edge glow. Checks passed: alpha samples on the transparent PNG showed edge/background alpha 0, `npm run lint`, `npm run build`, source search confirmed no old robot armature classes, and Playwright on fresh preview `http://185.125.103.25:3108` confirmed oldArmature=0, motionLayers=4, handPresses=4, panelGlows=4, one active glow/touch per phase, matching body/head transforms, and nonzero screenshot diffs across phases.

- 2026-07-02 latest homepage hero correction: after user rejection of the cube concept, the homepage hero now uses a generated/pererendered robot-dashboard image at `public/media/home/onixbit-robot-dashboard-hero.png` instead of the CSS cube/puzzle scene. `src/components/HomeHero.tsx` renders this single image through Next/Image and overlays four timed glow/touch zones for the existing left/right dashboard panels. The robot chest uses the red Onixbit logo mark in the generated artwork; the old cube classes are no longer referenced from the homepage hero. Important limitation: because the available artwork is one flattened PNG, the robot itself cannot physically rotate as an independent layer yet; only the panel glow/touch sequence is animated. For true 1:1 robot rotation, create separate robot/panel layers in Figma/PSD or generate transparent layer assets. Checks passed: `npm run check`, `PLAYWRIGHT_PORT=3150 npm run test:e2e:built` with 44 passed, asset HTTP 200 on preview, and source audit confirming robot classes only/no cube hero classes. Preview restarted at `http://185.125.103.25:3106`.
- 2026-07-02 correction after user review: the earlier homepage/service hero pass was corrected. The old homepage carousel slide structure from `HEAD:src/components/HomeHero.tsx` is now actually used on each service page hero (`/vnedrenie-bitrix24`, `/razrabotka-saitov-na-1c-bitrix`, `/raboty-po-1c-predpriyatie`) with the appropriate `direction` content and `ProductScene`, and without `ob-main-tabs` switching controls. The homepage cube animation was rebuilt again into one shared coordinate scene: SVG perspective grid, holes and cubes share positions, the old separate `ob-system-floor` and text captions are gone, cube motion uses transform-only keyframes, all three cubes hold in their holes before the completion wave, and measured stable center deltas are within about 1-3px. Checks passed: `npm run check`, `PLAYWRIGHT_PORT=3149 npm run test:e2e:built` with 44 passed, and custom Playwright desktop/mobile audits confirming service slide transfer, no tabs/captions/floor, no overflow, 3 cubes/18 faces/3 holes, and stable cube-slot alignment.
- 2026-07-02 homepage hero refinement: secondary red accents in the new system hero were moved to warm yellow/orange, including the top kicker and check icons. The CSS animation was rebuilt from flat pseudo-squares into larger CSS 3D cubes with six logo faces each, rounded soft-cube styling, deeper beveled holes, stronger lower-right-to-upper-left perspective, no floating text labels, slower fall/roll/drop timing, and a warm completion wave. Checks passed after the refinement: `npm run check`, `PLAYWRIGHT_PORT=3148 npm run test:e2e:built` with 44 passed, plus a custom Playwright desktop/mobile hero audit confirming 3 cubes, 18 faces, 3 holes, no old captions/tabs, no horizontal overflow, and warm secondary colors.
- 2026-07-02 homepage redesign first pass: replaced the old three-direction homepage carousel with a calmer static system hero in `src/components/HomeHero.tsx`. The new main message positions Onixbit as the team that connects CRM, site, and 1C into one working system; red is reserved for the key phrase, the primary CTA, small check icons, and the 1C-Bitrix cube. The right side uses a CSS 3D-like cube/puzzle animation with Bitrix24, 1C-Bitrix, and 1C logo cubes, reduced-motion handling, and no homepage slider tabs. `DirectionsSection` now has `id="directions"` for the secondary hero CTA. The three service pages keep their own `ProductScene` hero visuals without carousel controls. Checks passed: `npm run lint`, `npm run build`, and a Playwright desktop/mobile route audit for `/`, `/vnedrenie-bitrix24`, `/razrabotka-saitov-na-1c-bitrix`, and `/raboty-po-1c-predpriyatie` with zero horizontal overflow, no old homepage tabs, loaded cube logos, and verified animation state changes.
- 2026-07-02 AI workstation handoff: Aleksander is about to launch Codex on the stronger Windows 11 Pro PC for diagnostics, installation, and the main AI-assisted Onixbit workflow. Treat the next session's first user message `Начинаем этап 1: диагностика и установка` as the start of this sequence.
- Target strong PC known configuration: Windows 11 Pro preferred over Windows Server 2025; Intel Core i5-13600KF, 32 GB DDR5, Samsung M.2 SSD, NVIDIA GeForce RTX 2060. This is strong enough for Next.js/site work and lightweight local AI assistants; the RTX 2060 VRAM is the main limit for local models.
- Planned AI workflow: Codex remains the main engineer/orchestrator for Next.js, architecture, copy integration, animation, SEO, tests, and deployment. Figma/Figma AI is used as a visual drafting and review workspace, not as the final production site. Microsoft 365 Copilot is useful for briefs, service tables, content drafts, competitor notes, and presentations. Local models via Ollama/LM Studio act as free advisers for structure, copy, critique, screenshots, and coding hints.
- Preferred local AI stack to install/check on the strong PC: WSL2 Ubuntu with NVIDIA GPU visible via `nvidia-smi`; Node/npm/Git/Docker/VS Code/Codex CLI; Ollama; model candidates `qwen2.5-coder:7b`, `qwen3:8b`, `deepseek-r1:7b`, and a small visual model such as `qwen3-vl:4b` or `gemma3:4b`; Continue.dev in VS Code; optional Aider/OpenCode only after the base flow is stable.
- Installation order for the next strong-PC session: 1) safe diagnostics only, 2) fix WSL/GPU/driver visibility if needed, 3) install/update developer tools, 4) install Ollama and pull models, 5) connect VS Code/Continue/Figma MCP if useful, 6) audit the current Onixbit project, 7) start concept -> Figma review -> Next.js implementation -> Playwright/Lighthouse -> deploy.
- Important operating rule for the strong-PC setup: do not ask Aleksander for passwords/API keys in chat. Let him type logins, tokens, payment data, GitHub/Figma/Microsoft/server credentials, and Windows admin approvals locally when prompted.
- 2026-07-02 operating decision after local-auth check: keep Codex working from the existing `SSH: codex-germany` VS Code/Codex environment for Onixbit code, tests, project docs, and deployment work. Do not assume Codex can directly access Aleksander local Windows PC, installed Windows apps, RTX 2060, Figma/Office desktop state, or local Ollama models from this SSH session.
- Windows/strong-PC diagnostics and optimization should be guided manually: give Aleksander exact PowerShell/Windows/WSL commands to run locally, ask him to paste outputs, then recommend what to remove, disable, install, or tune. Do not request or copy Codex auth tokens such as `~/.codex/auth.json`; treat them like passwords.
- Local Windows Codex may be attempted only if the official `openai.chatgpt` VS Code extension can be installed locally and sign-in works from that machine. If it does not work because of region/network/auth limitations, continue with the hybrid workflow: Codex on `codex-germany` codes the site; Aleksander PC is used manually for Figma, Office Copilot, browser review, local AI tools, and system checks.
- Durability rule: keep all safe project work duplicated through Git/GitHub whenever practical, especially code, project docs, setup notes, design briefs, content guides, and handoff state. Never commit Codex auth caches, API keys, private SSH keys, real `.env` secrets, payment data, or provider tokens.
- Reminder rule: whenever a needed action cannot be performed from `codex-germany` because it requires Aleksander local Windows/WSL/GPU/desktop access, explicitly say so and provide a small copy-paste-safe PowerShell, Ubuntu, or VS Code step for Aleksander to run locally, then ask him to paste the output before proceeding.
- Long-term access plan: treat the current `codex-germany` environment as a temporary working channel. Aleksander wants an owned foreign dev server as the durable primary Codex/VS Code remote environment, with the local Windows/WSL machine as fallback and GitHub as the handoff source of truth. When planning this setup, distinguish technical connectivity from OpenAI account/billing/regional availability; do not ask for secrets in chat.
- 2026-07-02 AI cost-control workflow: continue using the current `codex-germany` Codex environment as the primary working channel for now. Aleksander-owned VPS/local Codex is the reserve/future primary path. Free local models on Aleksander PC via Ollama/LM Studio/Continue should be treated as manual draft advisers: Codex can provide prompts, Aleksander pastes outputs back, and Codex evaluates/implements. Default paid Codex model should be `gpt-5.4-mini` for coordination and routine implementation; escalate to `gpt-5.5` only for concept, architecture, difficult debugging, broad audits, or final high-stakes review.
- Figma workflow/security note: using a Figma account registered to a Yandex email is acceptable; the paid plan is tied to the Figma account/team, not to Google login. For the current third-party `codex-germany` environment, avoid storing long-lived Figma OAuth tokens unless Aleksander explicitly accepts the risk; prefer manual screenshots/links for now, or configure Figma MCP later on Aleksander-owned VPS/local Codex.
- Figma rollout plan: start with the free Figma account first for drafts, screenshots, links, and manual design review. Upgrade later only if Professional/Dev seat features are required for advanced Dev Mode, MCP Server handoff, higher AI credits, or smoother code handoff.
- Current Figma draft file: https://www.figma.com/design/LIrEHgK2w3gAoajMs3FWw1/Onixbit-redesign?node-id=1-5&t=t34AndFzdeB6HR1P-1. Initial test frame exists with Onixbit heading and service subtitle; until Figma MCP is configured, use screenshots or exported assets for feedback.
- Refined local AI install plan for Aleksander PC: use Ollama in WSL as the primary free local model runtime because the project already lives in WSL and NVIDIA is visible there. First models: `qwen2.5-coder:7b` for code review/small fixes, `gemma3:4b` for screenshot/image critique, `qwen3:4b` for fast Russian copy/structure drafts, and optional `deepseek-r1:7b` for slow second-opinion reasoning. Avoid 14B/32B models on RTX 2060 6GB except CPU experiments. Add Continue.dev in VS Code after Ollama is stable. LM Studio is optional GUI/OpenAI-compatible server, not first install. ComfyUI/image generation is postponed unless a real asset-generation task appears.
- Project root is clean React/Next.
- 2026-07-01/2026-07-04 redesign discovery note: before any further homepage/service/article visual redesign work, read `ONIXBIT_REDESIGN_BRIEF.md`. The earlier all-dark cinematic direction is now superseded by the hybrid decision: main public site stays mostly light and trust-building, while dark premium cinematic connected-system scenes move to separate process-demo pages. The current `/preview-onixbit-os` route is only a temporary playground, not an approved concept.
- 2026-07-01 full demo preview route: `/preview-onixbit-site` now contains a separate multi-page demo site with its own header, footer, cookie banner, homepage, three service pages, tariffs, cases, certificates, articles, about page, contacts, and privacy page. It is a safe demo shell while the production site is under moderation; do not publish or replace production pages from it without explicit approval. Checks passed: `npm run lint`, `npm run build`, and a Playwright browser audit across 8 routes x desktop/tablet/mobile with zero horizontal overflow and no JS errors. Current temporary tunnel from the discussion: `https://bdca7794f09543.lhr.life/preview-onixbit-site`.
- `onixbit-next` no longer exists.
- Old Tilda/preview/VK/archive materials were removed.
- `motion` is installed and ready.
- Playwright and axe are configured in `tests/` for smoke, overflow, health, and accessibility checks. `npm run browser:deps` prepares local Chromium libraries without sudo. `npm run test:e2e` builds first; `npm run test:e2e:built` expects an existing build.
- GitHub Actions CI and FirstVDS deploy checks build first, then run E2E tests against the built app.
- Lighthouse CI is configured in `lighthouserc.cjs` and runs manually with `npm run lhci`; it uses the same no-sudo local browser dependency wrapper.
- Sentry-ready files and `/api/health` exist; monitoring stays disabled until `NEXT_PUBLIC_SENTRY_DSN` is configured.
- Typography now uses self-hosted open-source Manrope for body text and Montserrat for headings, buttons, and accent UI.
- GitHub CLI `gh` is installed but not authenticated yet.
- Homepage budget animation refinements live in `src/app/home-budget-20260627.css`, imported after `globals.css` to force a fresh CSS chunk for cache-busting.
- `next.config.ts` sets `Cache-Control: public, max-age=0, must-revalidate` for `/_next/static/chunks/:path*` so CSS/JS chunk updates are revalidated instead of being stuck behind immutable browser cache during visual edits.
- 2026-06-28 session: completed a full-site audit/polish pass across the homepage, `/o-kompanii`, service pages, cases, articles, certificates, tariffs, contacts, privacy, sitemap, and global layout.
- Main changes from the full-site pass: stronger homepage positioning and quick service routes, more client-facing process copy, a first-call outcomes block on `/o-kompanii`, canonical/OpenGraph metadata on public pages, refined sitemap priorities/frequencies, mobile CTA compaction, and readable H1 text spacing for assistive tech.
- The temporary GetReview-like video widget is no longer mounted or imported in `src/app/layout.tsx` because the public external test video is not production-ready and the widget competed with mobile hero CTAs. Untracked files `src/components/VideoTrustWidget.tsx` and `src/app/video-trust-widget.css` may remain locally but should not ship unless replaced with an owned Onixbit video.
- Checks passed after the full-site pass: `npm run check`, `npm run lint`, `npm run build`, and `npm run test:e2e:built` with 26 passed. `next build` still prints the known custom Cache-Control warning for `/_next/static/chunks/:path*`.
- Playwright desktop/mobile screenshots for all public routes were regenerated in `output/playwright/audit-{desktop,mobile}-*.png`; route geometry checks reported no horizontal overflow.
- Local preview for the final build was started at `http://127.0.0.1:3100`.
- 2026-06-28 follow-up session: assembled and polished the three service pages `/vnedrenie-bitrix24`, `/razrabotka-saitov-na-1c-bitrix`, and `/raboty-po-1c-predpriyatie` with stronger decision, outcome, risk, and diagnostic CTA blocks.
- Same follow-up removed homepage hero quick-route buttons and decorative `01/02/03`-style counters from JSX/TSX surfaces, converted process markers to icons, restyled `/o-kompanii` proof teasers to match the homepage proof strip, and replaced the about-page route iframe with an interactive Yandex Maps API map. The map now uses coordinate-bound city placemarks, has no office-to-office route, and disables mouse-wheel zoom.
- Checks passed after the follow-up: `npm run check`, `npm run test:e2e:built` with 26 passed, and a custom Playwright DOM/runtime check confirming the homepage cleanup, service page blocks, about proof strip, route removal, and Yandex city labels. `npm run lhci` completed and wrote reports; `/contacts` still has a Lighthouse best-practices warning around third-party cookies/source maps/DevTools issues from the map stack plus moderate performance metrics. `next build` still prints the known custom Cache-Control warning for `/_next/static/chunks/:path*`.
- 2026-06-28 correction pass after user feedback: rebuilt the three service pages so they no longer share the same repeated block structure. `/vnedrenie-bitrix24` now uses a CRM command-center/workflow/automation composition, `/razrabotka-saitov-na-1c-bitrix` uses a browser blueprint/site architecture/release board composition, and `/raboty-po-1c-predpriyatie` uses an integration hub/responsibility boundary/exchange monitor composition. Checks passed: `npm run lint`, `npm run build`, `npm run check`, `npm run test:e2e:built` with 26 passed, and custom Playwright desktop/mobile DOM + screenshot checks for the three service routes with zero horizontal overflow.
- Planned `/certificates` direction from user feedback: make the hero a vivid interactive "штаб компетенций", not a dry document register. Show certificates as proof nodes in a broader project ecosystem: Bitrix24 competencies plus Wazzup/ChatApp messenger partners; 1C-Bitrix partnership/competencies plus Aspro and Concept template partners; SCloud partner certificate for 1C work. The page should keep trust and verification, but add imagination, motion, and a reason to stay on the site.
- Referral-link note for future CTAs: Aleksander has referral registration links for Bitrix24, a separate referral link for 1C-Bitrix, and a referral link for Scloud. When adding buttons for registration, licensing, partner services, or cloud/1C-related offers, ask for the exact URLs and use those referral links instead of generic partner links.
- 2026-06-28 certificates implementation: `/certificates` now uses an interactive "штаб компетенций" experience from `src/components/CertificatesExperience.tsx`, server-side folder scanning in `src/lib/certificate-assets.ts`, and page-specific styles in `src/app/certificates-experience.css`. Certificate PDFs are rendered to web previews by `scripts/generate-certificate-previews.mjs`; generated assets live in `public/media/certificates/_generated/manifest.json` and `.webp` files. Public previews mask detected validity-date lines only in generated web images; original uploaded PDFs/images remain untouched.
- 2026-06-29 deployment incident: production was temporarily healthy again but still running the previous `e0a615d...` image because the GitHub Actions deploy for `273d8a0...` failed during `Upload release archive`; the VPS was also filled mostly by Docker build cache/images. Manual cleanup reclaimed about 32 GB (`docker builder prune -af`, `docker image prune -af`), manual deploy published `273d8a0...`, and the deploy/rollback scripts now prune build cache plus unused images after successful runs.
- 2026-06-29 articles audit/context: `/articles` should become a client-facing Bitrix24 knowledge base, not a generic blog or editorial placeholder. Current page is technically healthy but still an announcement hub: article cards do not have individual URLs yet and lead to the lead form. Planned content model: section/series -> article -> goal -> steps -> screenshots/GIF/video when useful -> mistakes -> FAQ -> related articles -> CTA. `CONTENT_GUIDE.md` now exists in the repo root and is the source of truth for editorial voice, the `Записки интегратора` author persona, article templates, media rules, source rules, SEO/GEO requirements, CTA patterns, and proof/claim limits before writing or implementing articles.
- 2026-06-29 articles implementation: added `src/data/articles.ts`, rebuilt `/articles` as a real knowledge-base hub with series and roadmap, added the first SSG article at `/articles/poverhnostnoe-vnedrenie-bitrix24`, wired homepage article cards to real routes and roadmap, added article URLs to `sitemap.xml`, and extended Playwright smoke routes. First article uses real author attribution, FAQ, Article/FAQ/Breadcrumb JSON-LD, official Bitrix24 source links, and media guidance instead of fake screenshots. Checks passed: `npm run lint`, `npm run build`, `npm run test:e2e:built` with 32 passed, and `npm run check` with 0 audit vulnerabilities. The known Next custom Cache-Control warning still appears.

- 2026-06-30 articles expansion: `/articles` is now a real starter knowledge base with 7 published SSG articles: the existing surface-implementation manager checklist plus 6 new materials on CRM entities, website requests, deal visibility/access diagnostics, tasks/responsibility, deal robots, and the site-CRM-1C source-of-truth map. New article data lives in `src/data/article-extras.ts`; `src/data/articles.ts` now combines the original article with the additional set and keeps roadmap items for future releases.
- 2026-06-30 author visual decision: selected variant 6 (`onixbit-author-laptop-typing-01`) as the primary article author image, copied as `public/media/author/onixbit-author-selected.webp`. Supporting generated variants also live in `public/media/author/`; the durable editorial decision is documented in `CONTENT_GUIDE.md`.
- 2026-06-30 article template update: `src/app/articles/[slug]/page.tsx` supports per-article section titles while keeping Article/FAQ/Breadcrumb JSON-LD, dynamic metadata, SSG via `generateStaticParams`, source links, media guidance, mistakes, FAQ, and CTA blocks.
- 2026-06-30 test stability note: certificate modal E2E now waits for a client hydration marker on `.obx-certs` before clicking the preview. The component exposes `data-hydrated` via a ref after hydration and opens previews on pointer/click. Full checks passed: `npm run lint`, `npm run build`, `npm run check`, and `PLAYWRIGHT_PORT=3101 npm run test:e2e:built` with 44 passed. The known Next custom Cache-Control warning still appears.
- 2026-06-30 article UX/navigation polish: `/articles` was redesigned from a loose card board into a series-based knowledge library with a clear hero CTA, clickable rubric rail, grouped article sections, full-card article links, visible local article schemes, and lower-priority roadmap. Article pages now show a visual cover in the hero, a real media figure inside the media section, TOC link to media and next-step navigation, and previous/next article links. New local SVG assets live in `public/media/articles/`; visual mapping lives in `src/data/article-visuals.ts`. Checks passed: `npm run lint`, `npm run build`, `npm run check`, `PLAYWRIGHT_PORT=3104 npm run test:e2e:built` with 44 passed, and Playwright desktop/mobile screenshot/overflow checks for `/articles` plus `/articles/obrabotat-zayavku-s-saita-v-bitrix24` on port 3105.
- 2026-06-30 article audit/polish pass: `/articles` and all article pages now use contextual rubric themes, per-series action labels, subtle CSS-only reveal/hover animations with `prefers-reduced-motion` handling, and a clearly separated article `afterword` zone for sources, next reading, related links, and CTA. Article pages now visually distinguish the main instruction body from post-instruction blocks; mobile grid width was fixed so article body/afterword shrink to the viewport instead of being clipped by table min-width rules.
- Checks passed after the article audit/polish pass: `npm run lint`, `npm run build`, `npm run check` with 0 audit vulnerabilities, `PLAYWRIGHT_PORT=3120 npm run test:e2e:built` with 44 passed, SEO public-copy guard on article app/data files, and custom Playwright desktop/mobile screenshot + layout metrics for `/articles`, `/articles/poverhnostnoe-vnedrenie-bitrix24`, and `/articles/roboty-v-sdelkah-bitrix24` with zero horizontal overflow and verified `prefers-reduced-motion` CSS.
- Current untracked local files are expected and should not be shipped unless intentionally revived: `CODEX_SETUP_RU.md`, `author-reference-photos/`, `output/`, `src/components/VideoTrustWidget.tsx`, `src/app/video-trust-widget.css`, and `.work/`.

- 2026-06-30 article cleanup follow-up: removed public demo/demo-portal/obezlich wording from article copy, media captions, and SVG descriptions; article visuals are now presented as editorial working schemes without personal data. Fixed article-card image containment, card/title wrapping, article hero grid specificity, mobile H1 wrapping, and SVG text that could visually overflow.
- 2026-06-30 article mobile table fix: article warning/mistake tables now use `data-label` cells and collapse into readable row cards below 760px, so tables no longer expand the mobile body width.
- Checks passed after the cleanup follow-up: `npm run check`, `PLAYWRIGHT_PORT=3123 npm run test:e2e:built` with 44 passed, SEO public-copy guard, `git diff --check`, and a custom Playwright desktop/mobile audit across `/articles` plus every published article route with zero horizontal overflow, zero demo-copy hits, and no text/image overflow.
- 2026-07-01 articles redesign direction approved and implemented: `/articles` now uses a client-system diagnostic map (`Сайт -> CRM -> доступ -> задачи -> роботы -> 1С`) plus field-note cards with explicit situations. Individual article pages now show a hero diagnostic strip and a `Диагностическая карточка` before the main body, keeping the `Записки интегратора` editorial direction. Checks passed: `npm run check`, `PLAYWRIGHT_PORT=3141 npm run test:e2e:built` with 44 passed, `git diff --check`, and a custom Playwright desktop/mobile audit for `/articles` and `/articles/poverhnostnoe-vnedrenie-bitrix24` with zero horizontal overflow and verified reduced-motion handling.

## Working Storage Migration (2026-07-29)

- Historical local `tmp/` was moved atomically to ignored `.work/` in this exact contour; no working content was deleted.
- Active local script and documentation references were updated to `.work/`.
- New disposable tool files belong in `.work/scratch/`; accepted results belong in a semantic project folder.
